# Virtual environment
venv/
.venv/
env/
ENV/

# Python cache and temporary files
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ipynb_checkpoints/

# Operating System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Datasets and images
Dataset/
test_data/
SortedDataset/
*.heic
*.HEIC
*.jpg
*.jpeg
*.png

# Model checkpoints and logs that shouldn't be tracked (except best_model.pth if required, but model weights are usually ignored or tracked depending on policy. The user has best_model.pth inside the problem statement folder which they need. We shouldn't ignore it).
# Ignore CSV/logs created during runs
*.csv
*.log
